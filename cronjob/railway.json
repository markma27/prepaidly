{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "chmod +x ./gradlew && ./gradlew clean build -x test"
  },
  "deploy": {
    "startCommand": "java -jar build/libs/prepaidly-cronjob-0.1.0.jar",
    "restartPolicyType": "never"
  }
}
