{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "chmod +x cronjob/build.sh && ./cronjob/build.sh"
  },
  "deploy": {
    "startCommand": "java -jar cronjob/build/libs/prepaidly-cronjob-0.1.0.jar",
    "restartPolicyType": "never"
  }
}
