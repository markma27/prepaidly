{
  "name": "prepaidly",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "install": "npm install && cd frontend && npm ci"
  },
  "dependencies": {
    "next": "^14.2.35"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
